var object = {
    myString: 'string',
    myNumber: 10,
    myBoolean: false,
    myNull: null,
    myUndefined: undefined
};

var array = ['string', 10, false, null, undefined];

/* 將它與下列的原始值比較。在原始格式中，它們的值就像你看到的一樣簡單，但複合值可以封裝任何的 JavaScript 值 (以上的部分) */

var myString = 'string';
var myNumber = 10;
var myBoolean = false;
var myNull = null;
var myUndefined = undefined;